---
/**
 * Activity Gallery Component
 * Displays a grid of activity photos from the lab
 */
import { Image } from 'astro:assets';

interface Props {
	title?: string;
	subtitle?: string;
	images?: Array<{
		src: string;
		alt: string;
	}>;
	variant?: 'grid' | 'masonry';
}

const {
	title = '实验室活动',
	subtitle = '探索、创造、成长的精彩瞬间',
	images = [],
	variant = 'grid',
} = Astro.props;

// Default placeholder images - replace with actual activity photos
const defaultImages = [
	{ src: '/src/assets/images/default.png', alt: '学生进行科学实验' },
	{ src: '/src/assets/images/default.png', alt: '教育体系讲座' },
	{ src: '/src/assets/images/default.png', alt: '实验室研究项目' },
	{ src: '/src/assets/images/default.png', alt: '团队协作活动' },
	{ src: '/src/assets/images/default.png', alt: '创新设备展示' },
];

const displayImages = images.length > 0 ? images : defaultImages;
---

<section class="relative py-24 overflow-hidden">
	<div class="max-w-7xl mx-auto px-4 sm:px-6">
		<!-- Section Header -->
		<div class="text-center mb-16">
			<div class="font-mono text-xs text-primary-400 uppercase tracking-widest mb-4">
				[ ACTIVITY GALLERY ]
			</div>
			<h2 class="font-display font-black text-5xl md:text-6xl text-white mb-4">
				{title}
			</h2>
			<p class="text-lg text-gray-400 max-w-2xl mx-auto">
				{subtitle}
			</p>
		</div>

		<!-- Image Grid -->
		{variant === 'grid' ? (
			<div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
				{displayImages.map((image, index) => (
					<div
						class="group relative aspect-video overflow-hidden rounded-xl bg-dark-surface border border-white/5 hover:border-primary-500/30 transition-all duration-300"
						data-aos="fade-up"
						data-aos-delay={index * 100}
					>
						<div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity z-10"></div>
						<img
							src={image.src}
							alt={image.alt}
							class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
							loading="lazy"
						/>
						<div class="absolute bottom-0 left-0 right-0 p-6 z-20 translate-y-4 opacity-0 group-hover:translate-y-0 group-hover:opacity-100 transition-all duration-300">
							<p class="text-white font-medium text-sm">{image.alt}</p>
						</div>
					</div>
				))}
			</div>
		) : (
			<div class="columns-1 md:columns-2 lg:columns-3 gap-6 space-y-6">
				{displayImages.map((image, index) => (
					<div
						class="group relative break-inside-avoid overflow-hidden rounded-xl bg-dark-surface border border-white/5 hover:border-primary-500/30 transition-all duration-300"
						data-aos="fade-up"
						data-aos-delay={index * 100}
					>
						<div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity z-10"></div>
						<img
							src={image.src}
							alt={image.alt}
							class="w-full h-auto object-cover group-hover:scale-110 transition-transform duration-500"
							loading="lazy"
						/>
						<div class="absolute bottom-0 left-0 right-0 p-6 z-20 translate-y-4 opacity-0 group-hover:translate-y-0 group-hover:opacity-100 transition-all duration-300">
							<p class="text-white font-medium text-sm">{image.alt}</p>
						</div>
					</div>
				))}
			</div>
		)}

		<!-- Note about images -->
		<div class="mt-12 max-w-3xl mx-auto bg-primary-500/5 border border-primary-500/20 rounded-lg p-6">
			<div class="flex items-start gap-4">
				<div class="flex-shrink-0 w-10 h-10 bg-primary-500/10 border border-primary-500/30 rounded-lg flex items-center justify-center">
					<span class="text-primary-400 text-lg">📸</span>
				</div>
				<div class="flex-1">
					<h3 class="text-white font-display font-bold mb-2">关于活动图片</h3>
					<p class="text-gray-400 text-sm">
						请将活动照片保存到 <code class="px-2 py-1 bg-black/30 rounded text-primary-400 font-mono text-xs">src/assets/images/activities/</code> 目录，
						然后更新此组件的图片路径以显示真实的活动照片。
					</p>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	/* Masonry layout responsive adjustments */
	@media (max-width: 768px) {
		.columns-1 {
			columns: 1;
		}
	}
</style>
